{{- if not (lookup "v1" "Secret" (.Values.namespace | default .Release.Namespace) (printf "%s-secrets" .Release.Name)) }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-secrets
  namespace: {{ .Values.namespace | default .Release.Namespace }}
data:
  KEYCLOAK_ADMIN_USERNAME: {{ .Values.store.keycloak.adminUsername | b64enc }}
  KEYCLOAK_ADMIN_PASSWORD: {{ .Values.store.keycloak.adminPassword | b64enc }}
  KEYCLOAK_ADMIN_CLIENT: {{ .Values.store.keycloak.adminClient | b64enc }}
  KEYCLOAK_ADMIN_CLIENT_SECRET: {{ .Values.store.keycloak.adminClientSecret | b64enc }}
{{- end }}