apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-vantage6-server-deployment
  namespace: {{ .Values.namespace | default .Release.Namespace }}
  labels:
{{ include "vantage6.labels" . | indent 4 }}
    component: vantage6-server
spec:
  replicas: {{ .Values.server.replications }}
  selector:
    matchLabels:
{{ include "vantage6.matchLabels" . | indent 6 }}
      component: vantage6-server
  template:
    metadata:
      labels:
{{ include "vantage6.labels" . | indent 8 }}
        component: vantage6-server
    spec:
      containers:
      - name: vantage6-server
        image: {{ .Values.server.image }}
        env:
        - name: KEYCLOAK_URL
          value: {{ .Values.server.keycloakUrl }}
        - name: KEYCLOAK_REALM
          value: {{ .Values.server.keycloakRealm }}
{{- if and .Values.server.dev .Values.server.dev.store_in_local_cluster }}
        - name: VANTAGE6_CONFIG_LOCATION
          value: /mnt/config.yaml
        - name: LOCAL_STORE_URL
          value: http://vantage6-store-store-service.{{ .Values.namespace | default .Release.Namespace }}.svc.cluster.local:7602
{{- end }}
        envFrom:
        - secretRef:
            name: {{ .Release.Name }}-server-secrets
        ports:
        - containerPort: {{ .Values.server.internal.port }}
        command: [
          "gunicorn",
          "vantage6.server.wsgi:app",
          "--bind", "0.0.0.0:{{ .Values.server.internal.port }}",
          "--worker-class", "gthread",
          "--workers", "2",
          "--timeout", "120",
          "--graceful-timeout", "30"
        ]
        volumeMounts:
        - name: config-file
          mountPath: /mnt/config.yaml
          subPath: config.yaml

      volumes:
      - name: config-file
        configMap:
          name: {{ .Release.Name }}-vantage6-server-config