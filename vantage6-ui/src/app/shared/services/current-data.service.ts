import { Injectable } from '@angular/core';
import { ResType } from '../enum';
// import { ResTypeType } from './api.service';

@Injectable({
  providedIn: 'root',
})
export class CurrentDataService {
  // protected resource_type: ResType;
  // protected resource: ResourceType;
  // protected resource_list: ResourceType[] = [];
  // constructor(resource: ResourceType, resourc) {
  // }
}
