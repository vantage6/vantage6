summary: Get nodes from organization

description:
  Returns a list of nodes which are from the organization

parameters:
  - in: path
    name: id
    schema:
      type: integer
    description: organization id
    required: true

responses:
  200:
    description: Ok
  404:
    description: organization not found
  401:
    description: Unauthorized

security:
  - bearerAuth: []

tags: ["Organization"]
