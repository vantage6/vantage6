summary: Login node

description:
  Allows for node sign-in using a unique api-key.

requestBody:
  content:
    application/json:
      schema:
        $ref: '#/components/schemas/Node'
        

responses:
  200:
    description: Ok, authenticated
  400:
    description: No or wrong JSON-body, or invalid api-key provided

tags: ["Authentication"]