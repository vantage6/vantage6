summary: Login user or node

description:
  Allows for user and node sign-in. Users authenticate using a username and password combination and Nodes authenticate
  using a unique api-key.

#requestBody:
#  content:
#    application/json:
#      schema:
#        $ref: '#/definitions/Collaboration'

requestBody:
  content:
    application/json:
      schema:
        oneOf:
          - $ref: '#/components/schemas/User'
          - $ref: '#/components/schemas/Node'
        example:
          $ref: '#/components/schemas/User/example'
#
#\properties:
#            username:
#              type: string
#            password:
#              type: string
#          - properties:
#            api_key:
#              type: string
responses:
  200:
    description: Ok
  404:
    description: node with specified id is not found
  403:
    description: you do not have permission to view this node
  401:
    description: Unauthorized

tags: ["Authentication"]