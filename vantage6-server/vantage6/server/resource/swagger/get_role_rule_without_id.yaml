summary: Get role rules

description:
  Returns the role for rules without giving a user id
parameters:
  - in: path
    name: id
    schema:
      type: integer
      minimum: 1
    description: Role id
    required: true
responses:
  404:
    description: node with specified id is not found
  401:
    description: Unauthorized
security:
  - bearerAuth: []
tags: ["Role"]
